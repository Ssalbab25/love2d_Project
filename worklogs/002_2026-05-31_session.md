# 002 - 2026-05-31 - Session Log

## Session Meta
- Date: 2026-05-31
- Branch: main
- Base Commit (observed): 168841a
- Previous Worklog: 001_2026-05-30_session.md

## Session Update - 2026-05-31 (Windows Fixed Timestep)

### 이번 추가 작업
1. 고정 타임스텝 코어 추가
- `project/src/01_core/fixedTimestep.lua` 생성
- accumulator 패턴으로 1/60 고정 업데이트 지원
- frame spike clamp 및 max iteration 제한으로 spiral of death 방지

2. 메인 루프 연동
- `project/src/main.lua`에서 가변 dt 대신 고정 스텝으로 scene update 수행
- 입력 스냅샷은 프레임당 1회 수집 후 동일 스텝들에 사용

3. Windows 체감 확인용 디버그 표시 추가
- FPS, dt 표시를 draw에 추가해 끊김 체감 여부를 빠르게 점검 가능하게 구성

### 검증 기록
- editor diagnostics: `main.lua`, `fixedTimestep.lua`, `conf.lua` 오류 없음
- runtime: `run.bat` 실행 정상 종료 (Exit Code 0)

### 메모
- Windows 첫 실행에서 보인 미세한 끊김 체감은 재현되지 않음
- 추후 고속 모드/고속 볼에서 충돌 안정성 검증 기반으로 활용 예정

---

## Session Update - 2026-05-31 (Scene Flow + Level Select)

### 이번 추가 작업
1. 씬 흐름 확장
- `title -> mode select -> level select -> breakout -> pause/result` 흐름으로 정리
- 플레이 씬 래퍼 `breakoutScene`를 `project/src/03_game/scenes/` 아래로 이동
- `Backspace`를 뒤로가기 키로 사용하도록 mode/level/play/result/pause 경로 정리

2. 레벨 선택 씬 추가
- `project/src/03_game/scenes/levelSelectScene.lua` 생성
- 2열 그리드 + 미니맵 카드 형태로 레벨 미리보기 제공
- 선택 레벨을 유지한 채 gameplay로 진입하거나 이전 씬으로 복귀 가능하게 구성

3. 레벨 확장 및 시작 레벨 주입
- `project/src/03_game/levels.lua`에서 `classic`, `combo_rush`를 각 5레벨로 확장
- `project/src/03_game/breakout.lua`에서 `startLevel` 옵션을 받아 선택 레벨부터 시작하도록 변경

4. 입력 보강
- `project/src/03_game/input/inputAdapter.lua`에서 `space`, `return`, `kpenter`를 모두 launch 입력으로 처리
- 입력 하네스에 enter 발사 edge 케이스 추가

### 검증 기록
- `menu_flow_harness`: PASS
- `input_adapter_harness`: PASS
- `levels_set_harness`: PASS
- runtime: `run.bat` 실행 정상 종료 (Exit Code 0)

### 메모
- `Escape`는 종료 의미를 유지하고, 씬 뒤로가기는 `Backspace`로 분리
- 레벨 선택 UI는 이후 클리어 기록/잠금 상태를 얹기 쉬운 구조로 시작

---

## Session Update - 2026-05-31 (Progress Save + Level Meta UI)

### 이번 추가 작업
1. 진행도 저장 모듈 추가
- `project/src/03_game/progressStore.lua` 생성
- 모드별 `unlockedLevel`, 레벨별 `cleared`/`bestScore` 저장 구조 도입
- 저장 경로: `save/progress_v1.lua`

2. 게임 결과와 진행도 연동
- `project/src/03_game/scenes/breakoutScene.lua`에서 상태 전이(`level_clear`/`won`/`lost`) 기준으로 진행도 반영
- 레벨 선택으로 복귀 시 동일 progress store를 유지해 즉시 UI 반영

3. 레벨 선택 메타 정보 표시
- `project/src/03_game/scenes/levelSelectScene.lua`에서 `Unlocked x/y`, `LOCKED/OPEN/CLEAR`, `Best score` 표시
- 잠긴 레벨은 선택 가능하되 시작은 차단

4. 하네스 확장
- `project/tests/progress_store_harness.lua` 추가
- `project/tests/level_select_progress_harness.lua` 추가
- `project/tests/menu_flow_harness.lua`를 잠금 규칙 반영하도록 갱신

### 검증 기록
- `progress_store_harness`: PASS
- `level_select_progress_harness`: PASS
- `menu_flow_harness`: PASS

### 메모
- 하네스는 순수 Lua 환경에서도 동작하도록 progress store에 filesystem fallback 처리

---

## Session Update - 2026-05-31 (Mobile Scene Interaction + Guide)

### 이번 추가 작업
1. 씬 입력 이벤트 확장
- `project/src/01_core/sceneStack.lua`에 `touchpressed`, `mousepressed` 라우팅 추가
- `project/src/main.lua`에서 화면 좌표를 virtual 좌표로 변환해 씬으로 전달

2. 모바일 내비게이션/동작 지원
- `project/src/03_game/scenes/titleScene.lua`: 탭으로 다음 씬 진입
- `project/src/03_game/scenes/modeSelectScene.lua`: 탭 선택/재탭 시작 + 상단 BACK 버튼
- `project/src/03_game/scenes/levelSelectScene.lua`: 카드 탭 선택/시작 + 상단 BACK 버튼
- `project/src/03_game/scenes/breakoutScene.lua`: 상단 BACK/PAUSE 터치 버튼
- `project/src/03_game/scenes/pauseOverlayScene.lua`: 탭으로 resume
- `project/src/03_game/scenes/resultOverlayScene.lua`: 탭으로 restart, BACK 영역 탭으로 레벨 선택 복귀

3. 잠금 상태 경계값 보정
- `project/src/03_game/progressStore.lua`에서 `unlockedLevel` 하한을 1로 강제해 저장값 이상으로 인한 시작 불가를 방지

4. UI 가이드 병행 표시
- 키보드 가이드와 함께 모바일 가이드를 title/mode/level 씬에 병행 표기

### 검증 기록
- `menu_flow_harness`: PASS
- `progress_store_harness`: PASS
- `level_select_progress_harness`: PASS
- runtime: `run.bat` 실행 중 런타임 오류 없음

### 메모
- `Escape`는 종료, `Backspace`는 뒤로가기 유지
- 모바일에서는 탭 중심 진행이 가능하도록 모든 핵심 씬에 최소 인터랙션 경로 제공