# 001 - 2026-05-30 - Session Log

## Session Meta
- Date: 2026-05-30
- Branch: main
- Base Commit (observed): 168841a
- Important Previous Commit: e90f795 (breakout gameplay progression and juice)

## 이번 세션 주요 작업
1. 세로 모드 기준 전환
- 가상 해상도 기준을 540x1200으로 설정.
- conf.lua 기반 윈도우 설정 경로 확인.

2. 레벨 재설정
- 세로 모드에 맞춘 레벨 패턴으로 재구성.
- 레벨별 속도/테마/체력 벽돌 유지.
- 벽돌 배치 계산을 화면 비율 기반으로 보정.

3. 리팩터링 1단계
- 상태 상수화 및 상태 전이 경로 정리.
- 레벨 레이아웃 유효성 검증 추가.

## 현재 워킹트리 상태(요약)
- Modified:
  - project/src/03_game/breakout.lua
  - project/src/03_game/levels.lua
  - project/src/main.lua
- Renamed:
  - project/src/config.lua -> project/src/conf.lua
- Untracked:
  - project/src/01_core/

## 실행/검증
- 실행 커맨드: ./run.sh
- 태스크: Love2D run task 사용 가능
- 최근 실행 결과: 정상 종료(Exit Code 0)

## 결정 사항
- 아키텍처는 Hybrid 채택:
  - Scene Stack + Mode Strategy 중심
  - Flux는 메타/UI 상태에 부분 적용
  - ECS는 보류(필요 시 ECS-lite)

## 다음 세션 TODO (우선순위)
1. Input Adapter 도입 (keyboard/touch 통합)
2. Scene Stack 최소 골격 추가
3. Mode 인터페이스 분리 (classic부터)
4. 체인 시스템 MVP 추가

## 다른 디바이스/환경에서 이어서 시작 방법
1. 저장소 최신 pull
2. worklogs 최신 파일 확인
3. git status 확인
4. ./run.sh 또는 VS Code Love2D task 실행
5. TODO 1번부터 진행

## 로그 파일명 규칙
- 형식: NNN_YYYY-MM-DD_topic.md
- 예시: 002_2026-05-31_input-adapter.md
- 규칙:
  - NNN: 3자리 증가 번호(001, 002, 003 ...)
  - 날짜는 작업 시작일 기준
  - topic은 영어 소문자-kebab-case 권장
  - 기본 원칙: 일일 1파일만 생성하고, 같은 날짜의 추가 작업은 동일 파일에 누적 기록

---

## Session Update - 2026-05-30 (Later)

### 이번 추가 작업
1. 차별화 Phase 1 시작: 체인 시스템 MVP 적용
- combo 상태(카운트/타이머/배수) 도입
- 벽돌 점수에 배수 반영(+100/+25 기반 가중)
- 체인 만료/목숨 손실/레벨 전환 시 체인 초기화

2. UI 반영
- 상단 HUD에 Combo/Multiplier 표시 추가

3. 테스트 하네스 도입
- 순수 Lua 모듈 `project/src/03_game/combo.lua` 분리
- 하네스 파일 `project/tests/combo_harness.lua` 추가
- 하네스 우선 검증 수행(런타임은 최소 실행)

### 검증 기록
- Harness: `project/tests/combo_harness.lua` 통과
- Editor diagnostics: combo/breakout/hud/harness 파일 오류 없음
- Runtime: `run.sh` 1회 실행 확인

### 다음 액션
1. 입력 어댑터 분리 (keyboard/touch 공통 액션)
2. 체인 HUD 가독성 개선 (색/아이콘/만료 경고)
3. 체인 점수 규칙 하네스 케이스 확장

---

## Session Update - 2026-05-30 (Input Adapter)

### 이번 추가 작업
1. 입력 어댑터 도입
- `project/src/03_game/input/inputAdapter.lua` 신규 추가
- 키보드 입력을 공통 스냅샷(moveAxis, launchPressed, restartPressed, pausePressed)으로 정규화

2. 게임 루프 연동
- `project/src/main.lua`에서 매 프레임 adapter 업데이트 후 snapshot을 게임에 주입
- `project/src/03_game/breakout.lua`에서 직접 키보드 조회를 제거하고 snapshot 기반으로 이동/발사/재시작 처리

3. 하네스 추가
- `project/tests/input_adapter_harness.lua` 추가
- edge-trigger(launch/restart) 및 이동 축 동작 검증

### 검증 기록 (Harness First)
- `input_adapter_harness`: PASS
- `combo_harness` 회귀: PASS
- editor diagnostics: inputAdapter/main/breakout/harness 오류 없음
- runtime: `run.sh` 1회 실행

### 다음 액션
1. touch 입력 소스 추가(동일 adapter 인터페이스 유지)
2. sceneStack 최소 골격 도입
3. mode 인터페이스 분리(classic 1차)

---

## Session Update - 2026-05-30 (Scene Stack Skeleton)

### 이번 추가 작업
1. Scene Stack 코어 추가
- `project/src/01_core/sceneStack.lua` 생성
- push/pop/replace/top/update/draw/keypressed/setInputSnapshot/resize 지원

2. Breakout Scene 래퍼 추가
- `project/src/03_game/breakoutScene.lua` 생성
- 기존 Breakout을 씬 인터페이스로 감싸서 Stack에 탑재 가능하게 구성

3. main 루프 전환
- `project/src/main.lua`를 단일 game 인스턴스 호출에서 sceneStack 경유 호출로 변경
- 입력 스냅샷은 stack top scene에 주입

4. 하네스 추가
- `project/tests/scene_stack_harness.lua` 생성
- enter/exit, 호출 위임, replace 동작 검증

### 검증 기록 (Harness First)
- `scene_stack_harness`: PASS
- `input_adapter_harness` 회귀: PASS
- `combo_harness` 회귀: PASS
- editor diagnostics: sceneStack/breakoutScene/main/harness 오류 없음
- runtime: `run.sh` 1회 실행

### 다음 액션
1. mode 인터페이스 분리(classic 1차)
2. touch 입력 소스 추가(adapter 유지)
3. pause/result scene 오버레이 추가

---

## Session Update - 2026-05-30 (Mode Interface Split - Classic)

### 이번 추가 작업
1. Classic Mode 분리
- `project/src/03_game/modes/classicMode.lua` 생성
- 점수/콤보 규칙을 mode 훅(`onReset`, `update`, `onLifeLost`, `onLevelTransition`, `awardBrickPoints`)으로 분리

2. Breakout 연동
- `project/src/03_game/breakout.lua`에서 직접 Combo 호출을 제거하고 mode 훅 호출로 치환
- 코어는 물리/충돌/엔티티 상태 중심으로 유지

3. 하네스 추가
- `project/tests/classic_mode_harness.lua` 생성
- mode 훅 기반 점수/콤보 리셋 동작 검증

### 검증 기록 (Harness First)
- `classic_mode_harness`: PASS
- `combo_harness` 회귀: PASS
- `input_adapter_harness` 회귀: PASS
- `scene_stack_harness` 회귀: PASS
- editor diagnostics: classicMode/breakout/harness 오류 없음
- runtime: `run.sh` 1회 실행

### 다음 액션
1. touch 입력 소스 추가(adapter 인터페이스 유지)
2. pause/result scene 오버레이 추가
3. comboRush mode 스캐폴딩

---

## Session Update - 2026-05-30 (Touch Input Source)

### 이번 추가 작업
1. 터치 입력 소스 추가
- `project/src/03_game/input/touchInput.lua` 생성
- 터치 x 정규화(0..1) 기준으로 이동축(-1/0/1) 계산
- 터치 시작 edge를 `launchPressed`로 매핑

2. InputAdapter 병합
- `project/src/03_game/input/inputAdapter.lua`에서 keyboard + touch snapshot 병합
- 우선순위: 키보드 이동 입력이 있으면 키보드 우선, 없으면 터치 축 fallback
- 발사 입력은 keyboard/touch edge를 OR로 처리

3. 하네스 보강
- `project/tests/touch_input_harness.lua` 신규 추가
- `project/tests/input_adapter_harness.lua`에 touch 병합 케이스 추가

### 검증 기록 (Harness First)
- `touch_input_harness`: PASS
- `input_adapter_harness` 회귀: PASS
- `classic_mode_harness` 회귀: PASS
- `scene_stack_harness` 회귀: PASS
- editor diagnostics: touchInput/inputAdapter/harness 오류 없음
- runtime: `run.sh` 1회 실행

### 다음 액션
1. pause/result scene 오버레이 추가
2. comboRush mode 스캐폴딩

---

## Session Update - 2026-05-30 (Pause/Result Overlay Scenes)

### 이번 추가 작업
1. SceneStack 레이어링 확장
- `project/src/01_core/sceneStack.lua`
- push 시 scene에 stack 참조 주입
- draw/resize를 top-only에서 전체 stack 순회로 확장

2. 오버레이 scene 추가
- `project/src/03_game/scenes/pauseOverlayScene.lua` 생성
- `project/src/03_game/scenes/resultOverlayScene.lua` 생성

3. BreakoutScene 연동
- `project/src/03_game/breakoutScene.lua`
- pause 입력 시 pause overlay push
- won/lost 상태 진입 시 result overlay 1회 push

4. HUD 정리
- `project/src/04_ui/hud.lua`에서 won/lost 문구를 제거하고 결과 문구는 overlay가 담당

5. 하네스 보강
- `project/tests/scene_stack_harness.lua`에 stack 참조/레이어 draw/resize 검증 케이스 추가

### 검증 기록 (Harness First)
- `scene_stack_harness`: PASS
- `input_adapter_harness` 회귀: PASS
- `touch_input_harness` 회귀: PASS
- `classic_mode_harness` 회귀: PASS
- `combo_harness` 회귀: PASS
- editor diagnostics: scene/stack/hud/harness 오류 없음
- runtime: `run.sh` 1회 실행

### 다음 액션
1. comboRush mode 스캐폴딩

---

## Session Update - 2026-05-30 (comboRush Scaffold)

### 이번 추가 작업
1. 모드 스캐폴딩 추가
- `project/src/03_game/modes/comboRushMode.lua` 생성
- classic 대비 짧은 combo window, 높은 multiplier, 레벨 전환 보너스 점수 규칙 정의

2. 하네스 추가
- `project/tests/combo_rush_mode_harness.lua` 생성
- 기본 득점/배수 상승/타임아웃 리셋/레벨 전환 보너스 검증

### 검증 기록 (Harness First)
- `combo_rush_mode_harness`: PASS
- editor diagnostics: comboRush mode/harness 오류 없음
- runtime: `run.sh` 1회 실행

### 다음 액션
1. comboRush를 런타임에서 선택 가능하도록 mode 선택 설정 추가
2. touch 입력 UX 보강(드래그 기반 paddle 제어)

---

## Session Update - 2026-05-30 (Runtime Mode Selection)

### 이번 추가 작업
1. 모드 레지스트리 추가
- `project/src/03_game/modes/modeRegistry.lua` 생성
- `classic`, `combo_rush` 모드 팩토리와 fallback(`classic`) 정의

2. Breakout 모드 전환 API
- `project/src/03_game/breakout.lua`
- 생성자 옵션으로 `modeId` 수용
- `getModeId`, `setMode` 추가(전환 시 reset)

3. 런타임 입력 연결
- `project/src/03_game/breakoutScene.lua`
- `1` 키: classic, `2` 키: combo_rush 전환
- `project/src/main.lua`에서 기본 모드(`classic`) 전달

4. HUD 표시
- `project/src/04_ui/hud.lua`에 현재 모드와 단축키 안내 표시

5. 하네스 추가
- `project/tests/mode_registry_harness.lua` 생성

### 검증 기록 (Harness First)
- `mode_registry_harness`: PASS
- `combo_rush_mode_harness` 회귀: PASS
- `classic_mode_harness` 회귀: PASS
- `input_adapter_harness` 회귀: PASS
- `touch_input_harness` 회귀: PASS
- `scene_stack_harness` 회귀: PASS
- `combo_harness` 회귀: PASS
- editor diagnostics: 관련 파일 오류 없음
- runtime: `run.sh` 1회 실행

### 다음 액션
1. touch 입력 UX 보강(드래그 기반 paddle 제어)
2. 모드별 레벨/보상 규칙 차별화(데이터 테이블 분리)

---

## Session Update - 2026-05-30 (Touch Drag UX)

### 이번 추가 작업
1. 터치 입력 확장
- `project/src/03_game/input/touchInput.lua`
- snapshot에 `paddleTargetNorm` 추가
- 터치 유지 중 x 정규화 좌표를 drag 목표값으로 전달

2. 어댑터 병합 규칙 확장
- `project/src/03_game/input/inputAdapter.lua`
- keyboard 이동 입력이 없을 때만 touch `paddleTargetNorm` 전달
- keyboard axis가 있으면 target은 비활성화하여 충돌 방지

3. 패들 제어 개선
- `project/src/03_game/breakout.lua`
- `paddleTargetNorm`이 있으면 패들을 목표 x로 직접 이동(드래그 추적)
- target이 없을 때 기존 moveAxis 기반 제어 유지

4. 하네스 보강
- `project/tests/touch_input_harness.lua`
- `project/tests/input_adapter_harness.lua`
- target 필드 전달/우선순위 케이스 검증 추가

### 검증 기록 (Harness First)
- `touch_input_harness`: PASS
- `input_adapter_harness`: PASS
- `mode_registry_harness` 회귀: PASS
- `classic_mode_harness` 회귀: PASS
- `combo_rush_mode_harness` 회귀: PASS
- `scene_stack_harness` 회귀: PASS
- `combo_harness` 회귀: PASS
- editor diagnostics: 관련 파일 오류 없음
- runtime: `run.sh` 1회 실행

### 다음 액션
1. 모드별 레벨/보상 규칙 차별화(데이터 테이블 분리)
2. touch 조작감 미세 조정(스냅/감속 옵션) 실험

---

## Session Update - 2026-05-30 (Mode Balance Data Separation)

### 이번 추가 작업
1. 모드 밸런스 데이터 분리
- `project/src/03_game/modes/modeBalance.lua` 생성
- `classic`, `combo_rush`별 콤보 설정/레벨 클리어 보너스/레벨 속도 스케일 테이블 분리

2. 모드 생성 경로 정리
- `project/src/03_game/modes/modeRegistry.lua`에서 modeId별 튜닝 테이블 주입
- `classicMode`, `comboRushMode`는 레지스트리 없이 직접 생성해도 자체 기본 튜닝 로드

3. 레벨 로딩 시 모드 규칙 적용
- `project/src/03_game/breakout.lua`의 `loadLevel`에서 mode 훅(`applyLevelRules`) 호출
- comboRush는 레벨별 ball/paddle 속도 스케일 적용

4. 하네스 보강
- `project/tests/mode_balance_harness.lua` 신규 추가
- `project/tests/combo_rush_mode_harness.lua`에 레벨별 보너스/속도 스케일 검증 케이스 추가

### 검증 기록 (Harness First)
- `combo_rush_mode_harness`: PASS
- `mode_balance_harness`: PASS
- `mode_registry_harness` 회귀: PASS
- `classic_mode_harness` 회귀: PASS
- editor diagnostics: 관련 파일 오류 없음
- runtime: 이번 변경은 데이터/로직 분리 중심으로 기존 동일 루프 유지 (추가 실행 생략 가능)

### 다음 액션
1. touch 조작감 미세 조정(스냅/감속 옵션) 실험
2. comboRush 전용 레벨 셋 분리(난이도 곡선 명시)

---

## Session Update - 2026-05-30 (Mode-Specific Level Sets)

### 이번 추가 작업
1. 모드별 레벨 데이터 분리
- `project/src/03_game/levels.lua`를 단일 배열에서 모드 맵 구조로 전환
- `classic`, `combo_rush` 각각 독립 레벨셋/테마/패턴 정의

2. Breakout 연동
- `project/src/03_game/breakout.lua`에서 현재 modeId 기준 레벨셋 선택
- fallback은 `classic`으로 고정
- `maxLevel`도 선택된 레벨셋 기준으로 계산

3. 하네스 추가
- `project/tests/levels_set_harness.lua` 생성
- 모드별 레벨셋 존재/개수/패턴 차이 검증

### 검증 기록 (Harness First)
- `levels_set_harness`: PASS
- `mode_registry_harness` 회귀: PASS
- `mode_balance_harness` 회귀: PASS
- `combo_rush_mode_harness` 회귀: PASS
- `classic_mode_harness` 회귀: PASS
- editor diagnostics: levels/breakout/harness 오류 없음
- runtime: `run.sh` 1회 실행

### 다음 액션
1. touch 조작감 미세 조정(스냅/감속 옵션) 실험
2. 모바일 실기 체크 루틴 문서화

---

## Session Update - 2026-05-30 (Touch Feel Parameterization)

### 이번 추가 작업
1. 터치 추적 계산 모듈 분리
- `project/src/03_game/input/touchFollow.lua` 생성
- 스냅 거리(`snapDistance`), 응답 계수(`response`), 최대 속도(`maxSpeed`) 파라미터 기반 추적 계산

2. 모드별 터치 감각 설정 추가
- `project/src/03_game/modes/modeBalance.lua`
- `classic`, `combo_rush` 각각 `touchControl` 파라미터 정의

3. 브레이크아웃 연동
- `project/src/03_game/breakout.lua`
- 드래그 타겟 추적 시 `TouchFollow.step` 사용
- 현재 모드 튜닝에서 `touchControl`을 읽어 패들 추적 감각 적용

4. 하네스 보강
- `project/tests/touch_follow_harness.lua` 신규 추가
- `project/tests/mode_balance_harness.lua`에 touchControl 검증 추가

### 검증 기록 (Harness First)
- `touch_follow_harness`: PASS
- `mode_balance_harness` 회귀: PASS
- `levels_set_harness` 회귀: PASS
- `mode_registry_harness` 회귀: PASS
- `combo_rush_mode_harness` 회귀: PASS
- editor diagnostics: 관련 파일 오류 없음
- runtime: `run.sh` 1회 실행

### 다음 액션
1. 모바일 실기 체크 루틴 문서화
2. comboRush 후반 난이도(레벨 3) 미세 튜닝

---

## Session Update - 2026-05-30 (Mobile Real-Device Check Routine)

### 이번 추가 작업
1. 모바일 실기 점검 문서 추가
- `docs/003_2026-05-30_mobile-real-device-check-routine.md` 생성
- 입력 응답성/가독성/루프 안정성/모드 체감 차별화 기준 정의

2. 실기 템플릿 고정
- worklog에 붙여 쓸 PASS/FAIL 템플릿 포함
- FAIL 원인과 후속 액션 기록 규칙 명시

### 검증 기록
- 문서 작업(코드 변경 없음)
- 하네스/런타임 추가 실행은 생략

### 다음 액션
1. comboRush 후반 난이도(레벨 3) 미세 튜닝
2. 실기 1기기 체크 실행 후 템플릿 기반 기록

---

## Session Update - 2026-05-30 (Release Check Snapshot)

### 변경 파일
- 없음 (release-check는 검증 전용 실행)

### 런타임 위험 메모
- 입력/모드/Risk Lane 경로 모두 하네스 회귀 기준선 유지.
- macOS `IMKCFRunLoopWakeUpReliable` 메시지는 기존과 동일한 비차단 노이즈로 관찰.

### 검증 결과
1. Harness bundle: PASS
- `combo_harness`
- `input_adapter_harness`
- `touch_input_harness`
- `touch_follow_harness`
- `scene_stack_harness`
- `classic_mode_harness`
- `combo_rush_mode_harness`
- `mode_registry_harness`
- `mode_balance_harness`
- `levels_set_harness`
- `risk_lane_harness`

2. Runtime smoke: PASS
- `run.sh` 1회 실행

### 후속 항목
1. 실기 1기기 체크 템플릿 작성 완료(PENDING) 상태에서 결과 입력만 수행
2. 실기 FAIL 발생 시 해당 경로를 하네스 재현 케이스로 환원

---

## Session Update - 2026-05-30 (Special Bricks Pilot: Keyhole + Risk Core)

### 이번 추가 작업
1. 특수 벽돌 데이터 구조 도입
- `project/src/03_game/levels.lua`
- stage 단위 `specialBricks` 메타데이터 추가(`row,col -> {kind, hp}`)

2. 브레이크아웃 특수 벽돌 동작 연동
- `project/src/03_game/breakout.lua`
- `keyhole`: 파괴 시 `lock` 벽돌 일괄 해제
- `lock`: 해제 전에는 파괴 불가(충돌은 유지)
- `risk_core`: 파괴 시 Risk 토큰 추가 지급
- 특수 벽돌별 색상 렌더 반영

3. RiskLane 확장
- `project/src/03_game/riskLane.lua`
- 외부 효과용 `addTokens(amount)` 추가(캡/활성 상태 반영)

4. 시범 스테이지 적용
- `project/src/03_game/levels.lua` combo_rush level2에 keyhole/lock/risk_core 1세트 배치

5. 하네스 보강
- `project/tests/levels_set_harness.lua`
  - combo_rush level2 특수 벽돌 종류 존재 검증
- `project/tests/risk_lane_harness.lua`
  - `addTokens` 캡/비활성 동작 검증

### 검증 계획 (Harness First)
1. `levels_set_harness`
2. `risk_lane_harness`
3. 회귀(`mode_balance_harness`, `combo_rush_mode_harness`, `input_adapter_harness`) 실행

---

## Session Update - 2026-05-30 (Special Bricks Step 2: Lock Group Partial Unlock)

### 이번 추가 작업
1. 부분 해제 규칙 도입
- `project/src/03_game/breakout.lua`
- `lock` 벽돌에 그룹(`group`) 개념 추가
- `keyhole` 벽돌은 자신의 `unlockGroup`에 해당하는 `lock`만 해제

2. 시범 스테이지 확장
- `project/src/03_game/levels.lua` combo_rush level2
- lock 그룹 2종(`core`, `side`) + keyhole 2개 배치로 부분 해제 동선 구성

3. 레벨 하네스 강화
- `project/tests/levels_set_harness.lua`
- lock은 group 필수, keyhole은 unlockGroup 필수 검증
- keyhole unlockGroup이 실제 lock group과 매핑되는지 검증
- level2에 최소 2개 lock group 존재 검증

### 검증 계획 (Harness First)
1. `levels_set_harness`
2. 회귀(`risk_lane_harness`, `mode_balance_harness`, `combo_rush_mode_harness`) 실행

### 검증 기록 (Harness First)
- `levels_set_harness`: PASS
- `risk_lane_harness` 회귀: PASS
- `mode_balance_harness` 회귀: PASS
- `combo_rush_mode_harness` 회귀: PASS
- editor diagnostics: breakout/levels/levels_set_harness 오류 없음
- runtime: `run.sh` 1회 실행

### 다음 액션
1. classic 레벨에 소규모 keyhole/lock 샘플 추가해 모드 비교 테스트 가능 상태로 확장
2. lock group 단위 해제 연출(그룹명 또는 위치 힌트) 가독성 보강

---

## Session Update - 2026-05-31 (Title + Mode Select Scene)

### 이번 추가 작업
1. 타이틀 신 추가
- `project/src/03_game/scenes/titleScene.lua`
- SPACE/ENTER로 모드 선택 신으로 전환

2. 모드 선택 신 추가
- `project/src/03_game/scenes/modeSelectScene.lua`
- `1`/`2` 직접 선택 또는 UP/DOWN + ENTER로 선택
- 선택된 modeId로 BreakoutScene 생성

3. 앱 시작 흐름 전환
- `project/src/main.lua`
- 시작 시 Breakout 직행 대신 TitleScene 진입

4. 메뉴 흐름 하네스 추가
- `project/tests/menu_flow_harness.lua`
- Title -> ModeSelect -> Game 전이 검증

### 검증 계획 (Harness First)
1. `menu_flow_harness`
2. 회귀(`scene_stack_harness`, `mode_registry_harness`, `combo_rush_mode_harness`) 실행

---

## Session Update - 2026-05-31 (Keyboard + Mouse + Touch Input Integration)

### 이번 추가 작업
1. 마우스 입력 소스 추가
- `project/src/03_game/input/mouseInput.lua`
- moveAxis/paddleTargetNorm/launch edge 스냅샷 제공

2. InputAdapter 병합 확장
- `project/src/03_game/input/inputAdapter.lua`
- 우선순위: keyboard > mouse > touch
- launchPressed는 keyboard/mouse/touch edge OR 처리

3. 하네스 보강
- `project/tests/mouse_input_harness.lua` 신규 추가
- `project/tests/input_adapter_harness.lua`에 마우스 우선순위/launch edge 검증 추가

### 검증 계획 (Harness First)
1. `mouse_input_harness`
2. `input_adapter_harness`
3. 회귀(`touch_input_harness`, `touch_follow_harness`, `menu_flow_harness`) 실행

### 방향 정정
- 초기 구현에서 마우스 패들 이동을 포함했으나, 요구사항에 맞춰 다음 단계에서 마우스는 `서브 상태 초기 발사 각도 조준` 전용으로 전환.
- 패들 이동 입력은 기존처럼 keyboard/touch 기반 유지.

### 추가 보강
- 서브 상태 조준 가이드라인 길이를 확장하고, 좌/우 벽 및 상단 반사 경로를 예측선으로 표시.
- HUD serve 문구를 마우스 조준 + SPACE/클릭 발사 흐름으로 업데이트.

---

## Session Update - 2026-05-30 (comboRush Level 3 Fine-Tuning)

### 이번 추가 작업
1. comboRush 레벨 3 패턴 밀도 상향
- `project/src/03_game/levels.lua`
- 레벨 3의 3HP 벽돌 밀도를 높여 후반 압박 강화
- 레벨 3 기본 속도(`ballSpeed`) 상향, `paddleSpeed` 소폭 하향

2. 후반 스케일 튜닝
- `project/src/03_game/modes/modeBalance.lua`
- level3 `ballSpeedScale` 1.16, `paddleSpeedScale` 0.92로 조정

3. 하네스 강화
- `project/tests/levels_set_harness.lua`
  - level3 속도 상승/고체력 벽돌 밀도 증가 검증 추가
- `project/tests/combo_rush_mode_harness.lua`
  - level3 속도 스케일 기대값 검증 추가
- `project/tests/mode_balance_harness.lua`
  - level3 스케일 기대값 갱신

### 검증 기록 (Harness First)
- `levels_set_harness`: PASS
- `combo_rush_mode_harness`: PASS
- `mode_balance_harness`: PASS
- `risk_lane_harness` 회귀: PASS
- `mode_registry_harness` 회귀: PASS
- editor diagnostics: 관련 파일 오류 없음
- runtime: `run.sh` 1회 실행

### 다음 액션
1. 실기 1기기 체크 실행 후 템플릿 기반 기록
2. Risk Lane 피드백 강화(토큰 소모 시 강조 연출)

---

## Session Update - 2026-05-30 (Risk Lane Feedback Enhancement)

### 이번 추가 작업
1. 점수 보너스 피드백 강화
- `project/src/03_game/breakout.lua`
- Risk 토큰 소모 시 `RISK xN.NN` 팝업 추가
- 토큰 소모 순간 flash 타이머(`riskLaneFlash`)와 마지막 배수(`riskLaneLastMult`) 기록

2. 위험 구역 시각 강화
- `project/src/03_game/breakout.lua`
- flash 타이머에 따라 위험 구역 오버레이/경계선 알파를 일시적으로 강화

3. HUD 가독성 강화
- `project/src/04_ui/hud.lua`
- Risk Tokens에 flash 알파 반영
- 토큰 소모 직후 `Boost xN.NN` 표시

4. 하네스 보강
- `project/tests/risk_lane_harness.lua`
- risk lane 비활성 상태 동작(배수 미적용) 검증 케이스 추가

### 검증 기록 (Harness First)
- `risk_lane_harness`: PASS
- `mode_balance_harness` 회귀: PASS
- `combo_rush_mode_harness` 회귀: PASS
- `levels_set_harness` 회귀: PASS
- editor diagnostics: breakout/hud/risk_lane_harness 오류 없음
- runtime: `run.sh` 1회 실행

### 다음 액션
1. 실기 1기기 체크 실행 후 템플릿 기반 기록
2. 실기 결과에 따른 risk lane 파라미터 미세 조정

---

## Session Update - 2026-05-30 (Risk Lane Pulse/Sound Split)

### 이번 추가 작업
1. Risk 토큰 소모 전용 사운드 분리
- `project/src/03_game/breakout.lua`
- `riskConsume` 톤 추가 후 토큰 소모 시 재생

2. 컬러 펄스 경로 분리
- `project/src/03_game/breakout.lua`
- 위험 구역용 펄스(`riskLaneZoneFlash`)와 HUD용 펄스(`riskLaneHudFlash`)를 별도 타이머로 운용
- 공통 헬퍼 `triggerRiskConsumeFeedback`으로 소모 연출 경로 통합

3. HUD 펄스 연출 조정
- `project/src/04_ui/hud.lua`
- `riskLaneHudFlash` 기준으로 Risk Tokens/Boost 강조 표시 유지

### 검증 계획
1. 하네스 회귀: risk_lane/mode_balance/combo_rush_mode/levels_set
2. 런타임 스모크: `run.sh` 1회

### Mobile Real-Device Check - 2026-05-30
- Device: (기입 필요)
- OS: (기입 필요)
- Build Commit: 12228199be259d45ef7dd05579ab4c7c9533ea1a

#### A. 입력 응답성
- 최초 터치 반응: PENDING
- 드래그 추적 일관성: PENDING
- 가장자리 제어: PENDING

#### B. 가독성
- 손가락 가림: PENDING
- 상태 텍스트 가독성: PENDING

#### C. 루프 안정성
- 프레임 안정성: PENDING
- 상태 전이 안정성: PENDING

#### D. 모드 체감
- classic vs combo_rush 구분감: PENDING

#### Risk Lane 집중 항목 (오늘 빌드)
- 토큰 소모 사운드 즉시성: PENDING
- 위험구역/HUD 펄스 과포화 여부: PENDING
- combo_rush 레벨3 입력 품질: PENDING

#### 후속 액션
1. 실기 결과 기반 파라미터 조정 여부 결정
2. FAIL 항목 재현 조건을 하네스로 역추적 가능한지 검토

---

## Session Update - 2026-05-30 (Level3 Regression Harness 강화)

### 이번 추가 작업
1. combo_rush mode 하네스 강화
- `project/tests/combo_rush_mode_harness.lua`
- 레벨3 압박 프로파일(공/패들 상대 속도)이 레벨2보다 높아야 한다는 회귀 조건 추가
- combo_rush 고정 파라미터(window=1.1, hitsPerStep=3) 검증 추가

2. mode balance 하네스 강화
- `project/tests/mode_balance_harness.lua`
- combo_rush 핵심 파라미터(window/hitsPerStep) 고정 검증 추가
- 레벨3 스케일 방향성 검증 추가
  - ball scale: level3 > level2
  - paddle scale: level3 < level2

### 검증 계획 (Harness First)
1. `combo_rush_mode_harness` 실행
2. `mode_balance_harness` 실행
3. 관련 회귀(`levels_set_harness`, `risk_lane_harness`) 확인

---

## Session Update - 2026-05-30 (Risk Lane 수치 회귀 하네스 강화)

### 이번 추가 작업
1. Risk Lane 시퀀스 시뮬레이션 추가
- `project/tests/risk_lane_harness.lua`
- 실제 게임 호출 순서(`scoreWithBonus` -> `onBrickBreak`)를 반영한 고정 시퀀스 검증 추가

2. 체감 품질 수치 고정
- 혼합 시퀀스(위험/비위험 교차): 총점/보너스 히트 수/잔여 토큰 검증
- 밀집 위험 시퀀스(상단 연속 브레이크): 총점/보너스 히트 수/carry 토큰 검증

3. 튜닝 파라미터 안전성 검증
- tokenGain=2, consumePerHit=2 설정에서 2토큰 소비와 배수(2.0x) 적용 검증

### 검증 계획 (Harness First)
1. `risk_lane_harness` 실행
2. 회귀(`mode_balance_harness`, `combo_rush_mode_harness`, `levels_set_harness`) 실행

---

## Session Update - 2026-05-30 (입력 안정성 회귀 케이스 강화)

### 이번 추가 작업
1. keyboard/touch 충돌 입력 안정화
- [project/src/03_game/input/inputAdapter.lua](project/src/03_game/input/inputAdapter.lua)
- 좌/우 키 동시 입력처럼 keyboard intent가 있는 경우 touch fallback을 차단하도록 보강

2. input adapter 하네스 확장
- [project/tests/input_adapter_harness.lua](project/tests/input_adapter_harness.lua)
- keyboard conflict(좌+우 동시) 시 neutral 유지 및 touch target 차단 검증
- 키보드 해제 후 touch fallback 재개 검증

3. touch follow 하네스 확장
- [project/tests/touch_follow_harness.lua](project/tests/touch_follow_harness.lua)
- dt<=0 안정성(위치 유지/클램프) 및 exact target 정지 상태 검증

### 검증 계획 (Harness First)
1. `input_adapter_harness` 실행
2. `touch_follow_harness` 실행
3. 관련 회귀(`touch_input_harness`, `combo_rush_mode_harness`, `risk_lane_harness`) 실행

---

## Session Update - 2026-05-30 (Risk Lane MVP)

### 이번 추가 작업
1. Risk Lane 규칙 모듈 추가
- `project/src/03_game/riskLane.lua` 생성
- 상단 위험 구역 판정, 토큰 적립/소모, 보너스 배수 점수 계산 로직 분리

2. combo_rush 차별화 규칙 연동
- `project/src/03_game/modes/modeBalance.lua`에 `riskLane` 튜닝 추가
- classic은 비활성, combo_rush는 활성(토큰 기반 보너스)

3. 게임 루프/HUD 반영
- `project/src/03_game/breakout.lua`
  - 벽돌 파괴 시 위험 구역이면 토큰 적립
  - 다음 득점에서 토큰 소모 보너스 적용
  - 라이프 손실/레벨 전환 시 토큰 초기화
  - 배경에 위험 구역 시각화
- `project/src/04_ui/hud.lua`
  - Risk Tokens 표시

4. 하네스 추가/보강
- `project/tests/risk_lane_harness.lua` 신규 추가
- `project/tests/mode_balance_harness.lua`에 riskLane 설정 검증 추가

### 검증 기록 (Harness First)
- `risk_lane_harness`: PASS
- `mode_balance_harness` 회귀: PASS
- `combo_rush_mode_harness` 회귀: PASS
- `mode_registry_harness` 회귀: PASS
- `levels_set_harness` 회귀: PASS
- editor diagnostics: 관련 파일 오류 없음
- runtime: `run.sh` 1회 실행

### 다음 액션
1. comboRush 후반 난이도(레벨 3) 미세 튜닝
2. 실기 1기기 체크 실행 후 템플릿 기반 기록
